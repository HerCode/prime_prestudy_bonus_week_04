//Step 1: Create a project and add a 'Splinter' function.
    // a. The function should take in 2 numbers, multiply them together
    //    and be saved under a locally scoped variable called 'z'.
//Step 2: Create an array that stores the first, second arguments, and z.
//Step 3: Return that array.

//Step 4: Then console log out the Splinter function, passing in 1 and 2.

//Step 5: Check all of this in to a repo called "prime_prestudy_bonus_week_04"

function splinter(num 1, num 2) {
  var z = num 1 * num 2;
  var arr = ['num 1', 'num 2', 'var z'];
  return arr;
}

console.log(splinter(1, 2));
